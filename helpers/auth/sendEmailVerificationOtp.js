const sendEmailVerificationOtp = (otp, email) => {
  return new Promise((resolve,reject) => {
    // Sending Email for Email ID Verification logic goes here
    resolve(true);
  })
}

module.exports = {sendEmailVerificationOtp};